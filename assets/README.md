Place production app icons and splash assets here before store release.

The project intentionally does not include generated placeholder art because the UI uses native vector icons and does not require raster assets to run.
